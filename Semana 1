{
  "nbformat": 4,
  "nbformat_minor": 0,
  "metadata": {
    "colab": {
      "provenance": []
    },
    "kernelspec": {
      "name": "python3",
      "display_name": "Python 3"
    },
    "language_info": {
      "name": "python"
    }
  },
  "cells": [
    {
      "cell_type": "code",
      "execution_count": 11,
      "metadata": {
        "colab": {
          "base_uri": "https://localhost:8080/"
        },
        "id": "1owBh9FND9xf",
        "outputId": "f23d5b4b-00ca-45f8-930d-5336232eae75"
      },
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "Entrada=(0,0)  z=-2.0  salida=0\n",
            "Entrada=(0,1)  z=-1.0  salida=0\n",
            "Entrada=(1,0)  z=-1.0  salida=0\n",
            "Entrada=(1,1)  z=0.0  salida=1\n"
          ]
        }
      ],
      "source": [
        "import numpy as np\n",
        "\n",
        "def step(z):\n",
        "    return 1 if z >= 0 else 0\n",
        "\n",
        "def neurona(x1, x2, w1, w2, b):\n",
        "    z = x1*w1 + x2*w2 + b\n",
        "    y = step(z)\n",
        "    return z, y\n",
        "\n",
        "# Valores base\n",
        "w1, w2, b = 1, 1, -2.0  # intenta parecerse a una compuerta AND\n",
        "\n",
        "casos = [(0,0),(0,1),(1,0),(1,1)]\n",
        "for x1, x2 in casos:\n",
        "    z, y = neurona(x1, x2, w1, w2, b)\n",
        "    print(f\"Entrada=({x1},{x2})  z={z:.1f}  salida={y}\")\n"
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "**Pregunta1:**  Con que valor de b la neurona se comporta mas parecido a AND?\n",
        "**Respuesta:** Los valores con los que se comporta más parecido a una compuerta AND son -1.5 y -2.0 pero cuando unamos en el bias -2.0 estaria en el limite\n",
        "**Pregunta2:**Que efecto tiene bajar o subir el bias en la salida? (1-2 lineas?\n",
        "**Respuesta:** al subir el bias es decir que sea menos negativo se le es más facil a la neurona activarse y sucede lo contrario cuando se baja el bias a la neurona se le dificulta más activarse.\n"
      ],
      "metadata": {
        "id": "DZWbz8sHG9XN"
      }
    }
  ]
}
